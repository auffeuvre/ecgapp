<%= form_with(model: ecg) do |form| %>
  <% if ecg.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(ecg.errors.count, "error") %> prohibited this ecg from being saved:</h2>

      <ul>
        <% ecg.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :facts, style: "display: block" %>
    <%= form.text_area :facts %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
